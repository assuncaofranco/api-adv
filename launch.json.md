{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch built-in server and debug",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "runtimeExecutable": "php",
            "pathMappings": {
                "/var/www": "${workspaceFolder}"
            },
        }
    ]
}
